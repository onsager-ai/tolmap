{
  "//": "Read by scripts/collect-maps.mjs. `mapsDir` points at a directory of <stem>.json map files (the nine baseline names plus tolmap) generated WITH parcel polygons (`P`) so the 'plots' geometry mode has data for every repo, not just the two of nine committed fixtures that happen to carry `P`. The committed data/*.json fixtures are the acceptance corpus (byte-for-byte oracle for the Rust port) and are deliberately not touched to add a field they were not recorded with — this config points at a regenerated, complete copy instead. Precedence in collect-maps.mjs: $TOLMAP_MAPS_DIR env var, then this file's mapsDir, then ../data as the fresh-clone fallback (fewer geometry modes, but the app still runs).",
  "mapsDir": "/tmp/claude-1000/-home-marvin-projects-onsager-ai-tolmap/81dadee8-ac2c-4c03-9150-e9b50d4b60b6/scratchpad/maps"
}
