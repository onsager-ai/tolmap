The two map documents `scripts/check-view-stability.mjs` pins by SHA-256, gzipped. `public/maps/` is generated and gitignored, so without these copies CI had no way to supply them. The viewer-check workflow (`.github/workflows/viewer-check.yml`) unpacks them over the `collect-maps.mjs` output before it runs the check.

When the check's pinned hashes move (a map rebuilt from a newer pipeline), replace the file here in the same change.

`dify-baseline__dify.json.gz` is the perf-bench dots baseline (issue #82 B4 perf follow-up): `langgenius__dify.json.gz` with `P`/`footprint_centroids`/`file_neighbourhoods`/`neighbourhoods` stripped, so the SAME code renders it via the old dot path (`hasFootprints` is false) instead of footprint mode -- a same-code, same-underlying-map A/B rather than a separate old build. Served at `/dify-baseline/dify`, not a real repository. Regenerate it from a fresh `langgenius__dify.json.gz` the same way (strip those four keys) if that fixture is ever replaced; it is not itself pinned by `check-view-stability.mjs`.

`django__django.symbols.tar.gz` (1.1 MB, all 12 districts, 5.7 MB raw) and `langgenius__dify.symbols.tar.gz` (680 KB, district 0 only, 3.6 MB raw of dify's 24 MB full document -- issue #82 C2) are the per-district hierarchical-symbols sibling (docs/API.md's `<repo>.symbols/<district>.json` static layout), built from the SAME pipeline commit as the pinned map fixtures above -- both symbols documents' `map_sha256` in their build's `result.json` match `django__django.json.gz`'s and `langgenius__dify.json.gz`'s pinned hashes exactly, so file/symbol indices line up. The workflow unpacks each into `public/maps/<owner>/<repo>.symbols/<district>.json`. Only dify's district 0 is bundled (it has the largest file in the map, 175 top-level symbols, and a 51-member class -- everything the C2 checks/screenshots need for deep-zoom cards, class expansion and reference lines); the rest of dify's 78 districts are left out to keep the tarball well under the ~4 MB budget. Regenerate both the same way (`tolmap build --pkg ... --lang ...` writes `<name>.symbols/<district>.json` alongside the map) if the pinned map fixtures above are ever rebuilt -- a symbols fixture built from a different map commit than the one it's paired with will have file/symbol indices that no longer resolve.

Regenerated 2026-09-24 (#103, typed reference edges/abstract members) against a `remote-build.yml` run (`build`, `ref=main`) of the SAME pinned commit: both tarballs now carry the `kinds` legend, the trailing `abstract` column on every symbol row, and the fourth (`kind`) column on every edge -- neither map fixture above changed (`map_sha256`/SHA-256 verified byte-identical to the pinned hashes in `check-view-stability.mjs`'s own `MAP_FIXTURES`), only the symbols siblings did, since #103's Rust half (#104) only added columns to the symbols schema.
